services:
  tollgate:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - ./config.example.yaml:/app/config.yaml:ro
      - ./keys:/app/keys:ro
    depends_on:
      - httpbin
    restart: unless-stopped

  httpbin:
    image: kennethreitz/httpbin
    restart: unless-stopped
